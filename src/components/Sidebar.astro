---
interface Props {
  activeSection?: string;
}

const { activeSection = '' } = Astro.props;
---

<header class="w-full lg:w-[320px] xl:w-[360px] lg:h-screen lg:sticky lg:top-0 flex flex-col justify-end gap-6 lg:gap-8 py-8 lg:py-16 shrink-0 z-40 border-t lg:border-t-0 lg:border-r border-[var(--line)] lg:pr-8 order-last lg:order-first">
  <!-- Top Block: Identity Stack -->
  <div class="flex flex-col gap-6">
    <!-- Identity Stack in Exact Spec Order -->
    <div class="flex flex-col gap-1.5">
      {/* 1. abhiragh (Handwritten, large & bold) */}
      <a href="/" class="font-hand text-[48px] sm:text-[86px] font-bold text-[var(--ink)] leading-none tracking-tight hover:text-[var(--accent)] transition-colors inline-block select-none">
        abhiragh
      </a>

      {/* 2. Cloud Engineer (Normal type, smaller, directly under name) */}
      <h2 class="font-serif text-[17px] sm:text-[18px] text-[var(--ink)] font-normal leading-snug">
        Cloud Engineer
      </h2>

      {/* 3. One-line tech/identity summary (reusing top tags) */}
      <div class="font-mono text-[11px] text-[var(--ink-tertiary)] tracking-[0.1em] uppercase font-semibold mt-1">
        AWS &middot; Linux &middot; Docker
      </div>

      {/* 4. Kerala, India (Small normal/mono type) */}
      <div class="font-mono text-[11px] text-[var(--ink-tertiary)] tracking-[0.06em]">
        Kerala, India
      </div>
    </div>

    <!-- 5. Nav Links (Primary Navigation) -->
    <nav class="flex flex-wrap lg:flex-col gap-3 lg:gap-3.5 mt-4 lg:mt-6 font-mono text-[11px] uppercase tracking-[0.12em] text-[var(--ink)]">
      <a
        href="/#experience"
        class="group flex items-center gap-3 text-[var(--ink)] hover:text-[var(--accent)] transition-colors py-1"
      >
        <span class="h-[1px] w-5 bg-[var(--ink-tertiary)] group-hover:w-8 group-hover:bg-[var(--accent)] transition-all hidden lg:inline-block"></span>
        <span>EXPERIENCE</span>
      </a>
      <a
        href="/projects"
        class="group flex items-center gap-3 text-[var(--ink)] hover:text-[var(--accent)] transition-colors py-1"
      >
        <span class="h-[1px] w-5 bg-[var(--ink-tertiary)] group-hover:w-8 group-hover:bg-[var(--accent)] transition-all hidden lg:inline-block"></span>
        <span>PROJECTS</span>
      </a>
      <a
        href="/archive"
        class="group flex items-center gap-3 text-[var(--ink)] hover:text-[var(--accent)] transition-colors py-1"
      >
        <span class="h-[1px] w-5 bg-[var(--ink-tertiary)] group-hover:w-8 group-hover:bg-[var(--accent)] transition-all hidden lg:inline-block"></span>
        <span>COLLECTION</span>
      </a>
      <a
        href="/blogs"
        class="group flex items-center gap-3 text-[var(--ink)] hover:text-[var(--accent)] transition-colors py-1"
      >
        <span class="h-[1px] w-5 bg-[var(--ink-tertiary)] group-hover:w-8 group-hover:bg-[var(--accent)] transition-all hidden lg:inline-block"></span>
        <span>WRITING</span>
      </a>
    </nav>
  </div>

  <!-- 6. Bottom-anchored within column: github, linkedin, email & Theme Toggle -->
  <div class="flex flex-col gap-3 mt-8 lg:mt-0 font-mono text-[11px] tracking-[0.08em] text-[var(--ink-secondary)] select-none">
    <div class="flex items-center gap-3">
      <a href="https://github.com/abhiragh" target="_blank" rel="noopener noreferrer" class="hover:text-[var(--ink)] transition-colors lowercase">
        github
      </a>
      <span>&middot;</span>
      <a href="https://linkedin.com/in/abhiragh" target="_blank" rel="noopener noreferrer" class="hover:text-[var(--ink)] transition-colors lowercase">
        linkedin
      </a>
      <span>&middot;</span>
      <a href="mailto:contact@abhiragh.com" class="hover:text-[var(--ink)] transition-colors lowercase">
        email
      </a>
    </div>

    {/* Minimal Plain Text Theme Toggle Button */}
    <div class="pt-0.5">
      <button
        id="theme-toggle-btn"
        type="button"
        class="font-mono text-[11px] text-[var(--ink-secondary)] hover:text-[var(--accent)] transition-colors cursor-pointer inline-flex items-center gap-1.5 uppercase tracking-[0.1em]"
        aria-label="Toggle theme mode"
      >
        <span id="theme-toggle-icon" class="text-[12px]">☾</span>
        <span id="theme-toggle-label">DARK</span>
      </button>
    </div>
  </div>
</header>

<script is:inline>
  (function() {
    function bindThemeToggle() {
      const btn = document.getElementById('theme-toggle-btn');
      const icon = document.getElementById('theme-toggle-icon');
      const label = document.getElementById('theme-toggle-label');

      function updateUI(theme) {
        if (icon && label) {
          if (theme === 'dark') {
            icon.textContent = '☾';
            label.textContent = 'DARK';
          } else {
            icon.textContent = '☀';
            label.textContent = 'LIGHT';
          }
        }
      }

      const initialTheme = document.documentElement.getAttribute('data-theme') || 'dark';
      updateUI(initialTheme);

      if (btn && !btn.dataset.bound) {
        btn.dataset.bound = 'true';
        btn.addEventListener('click', () => {
          const current = document.documentElement.getAttribute('data-theme') === 'dark' ? 'light' : 'dark';
          document.documentElement.setAttribute('data-theme', current);
          localStorage.setItem('theme', current);
          updateUI(current);
          window.dispatchEvent(new CustomEvent('themechange', { detail: { theme: current } }));
        });
      }
    }

    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', bindThemeToggle);
    } else {
      bindThemeToggle();
    }
  })();
</script>
